These scripts all require nusoap.php.  I have included a copy that all of these scripts are tested with.

There are known bugs if their is only one item being returned.  

For example you only have one show scheduled, one show on your VOD server (or one show in a project if your restricting).  Basically try to have lots of shows :)


-ray